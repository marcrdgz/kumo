---
title: Configuration
description: The config.toml reference — keymaps, shells, agents, and file locations.
icon: ⚙️
---

import { TypeTable } from 'fumadocs-ui/components/type-table';
import { Callout } from 'fumadocs-ui/components/callout';

Kumo reads a single config file at `~/.config/kumo/config.toml`. The old flat
`key = value` file at `~/.config/kumo/config` (and the legacy `~/.kumo`)
still reads as a fallback, but `config.toml` wins when both exist.

```toml
# ~/.config/kumo/config.toml

ai-cmd = "claude --model sonnet"
update-check = true

[terminal]
shell = "/bin/zsh"

[keymap]
leader = "ctrl+b"

# Leader bindings are remappable: override a stock key or add a new one.
[keymap.bindings]
s = "split-vertical"   # now leader+s splits vertically
x = "close-pane"

[notifications]
position = "top-right"  # top-left | bottom-right | bottom-left | center | off
sound = true            # audible chime on blocked/finished transitions

[worktree]
shared-dirs = ["node_modules", ".cache"] # gitignored dirs shared by worktrees
expose-socket = true    # pass KUMO_SOCKET_PATH and KUMO_BIN_PATH to panes
```

## Config builder

<ConfigBuilder />

The generator writes only what differs from the defaults, in canonical form.
Paste the result into `~/.config/kumo/config.toml`; the daemon reloads it
automatically. `kumo reload` remains available for an explicit refresh.

## Install the agent skill

`ai-cmd` chooses which AI CLI Kumo starts. Install Kumo's bundled skill in that
agent as a separate, one-time step so it can discover panes, wait without
polling, read terminal output, and coordinate other agents. Choose your agent
and run the generated command:

<AgentSkillInstaller />

The skill is embedded in the `kumo` binary, so the installed instructions match
your CLI version. Re-run the same command after updating Kumo to refresh it.
`kumo agent skill` without `--output` prints the skill instead.

You can manage the same targets from **MENU → settings → agents**. Select an
agent and press Enter (or click it) to install the skill; selecting an installed
skill removes its `SKILL.md`. **Update available** means the file exists but its
contents differ from the skill bundled with the running Kumo version—usually
because Kumo was upgraded, but also possibly because the file was edited. The
update action replaces that file with the bundled version.

## Worktree isolation

The optional `[worktree]` section controls what new isolated worktrees share
with the primary checkout:

| Key | Description |
| --- | --- |
| `shared-dirs` | Relative, gitignored directories to link into each new worktree (for example `node_modules` or `.cache`). Absolute paths and paths containing `..` are ignored. Missing, non-directory, or non-gitignored sources are skipped with a warning. |
| `expose-socket` | When `true` (the default), panes receive `KUMO_SOCKET_PATH` and `KUMO_BIN_PATH` so an agent can drive its own Kumo session. |

For files that should be copied rather than shared, add a `.worktreeinclude`
file at the repository root. Put one literal, relative, gitignored path per
line; blank lines and lines beginning with `#` are ignored. Globs, negations,
absolute paths, and paths containing `..` are skipped. For example:

```text
# Local configuration copied into each isolated checkout
.env
.vscode/settings.json
```

These paths are copied when `kumo worktree create --ai` creates a checkout.

The current 0.7 flow starts an agent when `--agent` is supplied, but does not
send a task automatically. After the agent is ready, submit work explicitly
with `kumo agent prompt`; automatic task submission is planned for 0.7.1.

## Reference

<TypeTable
  type={{
    'keymap.leader': {
      description:
        'Leader chord that enters leader mode (`ctrl+b`, `ctrl+space`, `f12`, …). A top-level `leader` also reads as a deprecated alias.',
      default: '`ctrl+b`',
    },
    'keymap.bindings': {
      description:
        'Overrides/additions to the leader bindings: `chord = "action-id"` (e.g. `s = "split-vertical"`). Invalid chords/actions are ignored with a warning.',
      default: 'stock keymap',
    },
    'ai-cmd': {
      description:
        'AI CLI to run in the AI pane (program + args, space-separated). `ai_cmd` also works for back-compat.',
      default: '`opencode`',
    },
    shell: {
      description:
        'Login shell used to spawn panes. Canonical home is `[terminal] shell`; a top-level `shell` also reads as a deprecated alias.',
      default: '`$SHELL` → `/bin/zsh`',
    },
    'sidebar.layout': {
      description:
        'Sidebar arrangement: `project` (default, grouped worktrees with inline agents), `divided` (stacked spaces + agents), or `tabs` (classic two-tab). `explorer`/`navigator` are aliases of `project`.',
      default: '`project`',
    },
    'sidebar.width': {
      description:
        'Width of the sidebar in columns when `layout = "project"` (draggable live, clamped 20..50). Honored on `kumo reload`.',
      default: '`28` (`25` for other layouts)',
    },
    'worktree.shared-dirs': {
      description:
        'Relative, gitignored directories to share with new isolated worktrees (for example `node_modules` or `.cache`).',
      default: 'empty',
    },
    'worktree.expose-socket': {
      description:
        'Pass `KUMO_SOCKET_PATH` and `KUMO_BIN_PATH` to processes spawned in panes.',
      default: '`true`',
    },
    'update-check': {
      description:
        'Whether the startup update check runs (also `KUMO_NO_UPDATE=1`).',
      default: '`true`',
    },
    'notifications.position': {
      description:
        'Where agent toasts anchor in attached viewers: `top-right`, `top-left`, `bottom-right`, `bottom-left`, or `center`. `off` (also `never`) silences toasts entirely — the chime is unaffected (also `KUMO_NO_NOTIFY=1`).',
      default: '`top-right`',
    },
    'notifications.blocked': {
      description: 'Toast when a working agent becomes blocked.',
      default: '`true`',
    },
    'notifications.finished': {
      description: 'Toast when a working agent finishes / goes idle (`idle` and `done` are aliases).',
      default: '`true`',
    },
    'notifications.sound': {
      description:
        'Whether blocked/finished agent transitions play an audible chime (`alert-sound`/`alert_sound` also work; also `KUMO_NO_SOUND=1`).',
      default: '`true`',
    },
    'agent-sound': {
      type: 'deprecated',
      description:
        'Deprecated top-level alias of `[notifications] sound`. The `[notifications]` key wins when both are set.',
      default: '`true`',
    },
  }}
/>

## File locations

Overrides follow the usual conventions:

| Location | Path / override |
| --- | --- |
| Config | `~/.config/kumo/config.toml` — override with `KUMO_CONFIG_DIR` or `XDG_CONFIG_HOME` |
| Runtime state | `~/.local/state/kumo/` — override with `KUMO_STATE_DIR` or `XDG_STATE_HOME` |
| IPC socket | `$XDG_RUNTIME_DIR/kumo` |

## Live reload

The daemon watches the canonical, flat, and legacy config files plus
`agent-detection/*.toml`, then applies the same reload path automatically.
`kumo reload` (or MENU → `reload`) triggers it explicitly. Reload applies `shell`,
`ai-cmd`, `leader`, and `keymap.bindings` live to panes spawned from then on.
`new-cwd`, `[notifications] position`, and `[notifications] sound` apply
instantly. MENU → `config` opens the config
file in an editor pane inside the session (`$VISUAL` → `$EDITOR` → `vi`).
